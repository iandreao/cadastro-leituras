import UnidadeScreen from "@/components/UnidadeScreen";

export default function UnidadesPage() {
  return (
    <UnidadeScreen
      condominiosIniciais={[]}
      unidadesIniciais={[]}
      tiposIniciais={[]}
    />
  );
}
